# Application Configuration
domain: thesistrack.dev
environment: production
projectName: "Thesis Tracker"

# SSL Configuration
ssl:
  email: your-email@example.com  # Change this to your email for Let's Encrypt

# Database Configuration
postgres:
  image: postgres:15
  storage: 1Gi
  service:
    name: postgres
  credentials:
    user: thesistrack_user
    password: thesistrack_pass  # This will be stored in secrets
    database: thesistrack_db
  port: 5432

# Backend Configuration
backend:
  image:
    repository: thesistrackregistry.azurecr.io/thesis-backend
    tag: latest
  port: 8000

# Frontend Configuration  
frontend:
  image:
    repository: thesistrackregistry.azurecr.io/thesis-frontend
    tag: latest
  port: 5173

# Secrets Configuration
secrets:
  secretKey: "change-this-in-production-use-a-secure-random-key"  # Change this!
  googleClientId: "your-google-client-id.apps.googleusercontent.com"  # Add your Google Client ID
  googleClientSecret: "your-google-client-secret"  # Add your Google Client Secret

# Email Configuration (Optional)
email:
  smtpHost: ""  # e.g., smtp.gmail.com
  smtpPort: "587"
  smtpUser: ""  # your email
  smtpPassword: ""  # your email password (stored in secrets)
  fromEmail: ""  # your email
  fromName: "ThesisTrack"
